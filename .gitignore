# Environment and secrets — never commit
.env
.env.local
.env.*.local

# Node
node_modules/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# IDE
.idea/
.vscode/
.claude/
*.swp
*.swo

# Logs and data
logs/
data/*.db

# Source/backup text files
backup.txt
commander.txt
media.txt
webhook.txt
whatsapp.txt
response A.txt
Response B.txt

# Sensitive data - never commit
data/woocommerce/
data/revenue/
data/backups/
data/goals/
data/customer_winback/
data/health_audit/
data/monthly_goals.json
data/daily_progress.json
data/costs.json.bak
data/revenue.json.bak
nul
scripts/migrate_revenue.py

# Test output artifacts — not for version control
full_test_report.txt
test_out.txt

# AEO scan data — contains API responses, not for version control
data/aeo/

# Pricing scan data — contains scraped prices, not for version control
data/pricing/

# SSH keys — never commit
*.key
ssh-key*
