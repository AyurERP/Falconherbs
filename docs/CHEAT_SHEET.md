# 🦅 FALCON AGENCY — WHATSAPP COMMAND CHEAT SHEET
*Send these messages to WhatsApp number: +91 99163 22917 (your agency)*

---

## ⚡ INSTANT COMMANDS (One-liner, fires immediately)

| What you want | WhatsApp message |
|---|---|
| Full status update | `sab batao` / `kya chal raha hai` / `update do` |
| Today's summary | `aaj ka summary` / `evening report` |
| Morning briefing | `morning report` / `subah ka report` |
| Check site uptime | `site up?` / `check uptime` |
| Check orders | `kitne order aaye` / `orders check` |
| Revenue report | `kitna kamaya` / `revenue report` / `kamai` |
| Profit report | `kitna profit` / `profit report` |
| Goal progress | `progress dikhao` / `kitna hua` |
| Content drafts | `drafts dikhao` / `content status` |

---

## 🛡️ HEALTH CLAIM FIXES (3-Step Flow)

```
Step 1 → "scan products"          ← Scans all 87 products (2-3 min)
Step 2 → "rewrite products"       ← AI rewrites flagged ones
Step 3 → "push karo"              ← Applies rewrites to live site ✅

OR: "sab fix karo"                ← Does products + blogs + pages + categories in one shot
```

| Command | What it does |
|---|---|
| `scan products` | Scan 87 products for health claim violations |
| `rewrite products` | AI-rewrite flagged product descriptions |
| `push karo` | Apply product rewrites to live site |
| `scan blogs` | Scan 46 blog post titles |
| `rewrite blogs` | AI-rewrite flagged blog titles |
| `blogs fix karo` | Push blog title fixes to site |
| `scan pages` | Scan WP pages (About, Home, etc.) |
| `rename categories` | Fix risky category names (e.g., Diabetic → Wellness) |
| `sab fix karo` | Apply ALL pending fixes (products+blogs+pages+categories) |
| `rewrite status` | How many rewrites are pending/done |
| `changelog report` | Get full before/after change report file |
| `health scan` | Quick compliance check of whole site |
| `is this safe: [text]` | Check if a specific phrase is safe to use |

---

## ✍️ CONTENT CREATION

| Command | What it does |
|---|---|
| `blog likh [topic]` | Generate blog post on topic (e.g., "blog likh about ashwagandha") |
| `social post bana [topic]` | Create Instagram/Facebook post |
| `weekly content` | Generate full week of content |
| `content calendar bana` | Generate 30-day content calendar |
| `video script likh [topic]` | Write video/reel script |
| `publish karo` | Publish latest approved draft to WordPress |
| `drafts dikhao` | List all draft content waiting for review |
| `preview draft [number]` | Preview a specific draft |
| `reject draft [number]` | Reject and delete a draft |

---

## 📊 ANALYTICS & SEO

| Command | What it does |
|---|---|
| `analytics traffic` | Google Analytics traffic report |
| `seo audit` / `seo karo` | Full SEO audit of site |
| `competitor check` | Competitor analysis |
| `keyword analysis` | Keyword research |
| `price scan` | Scan competitor prices on Amazon |
| `price report` | Last price scan results |
| `ads status` | Google/Meta ads performance |

---

## 💾 BACKUP & SECURITY

| Command | What it does |
|---|---|
| `backup banao` / `backup le` | Create site backup |
| `backup dikhao` | List available backups |
| `backup verify` | Check backup integrity |
| `security scan` / `scan site` | Full security check |
| `sentry check` | Check error monitoring |

---

## 🏪 STORE / WOOCOMMERCE

| Command | What it does |
|---|---|
| `store audit` / `dukaan check` | Full WooCommerce audit |
| `kitne products` / `inventory` | Product & inventory status |
| `payment gateway check` | Check Razorpay/payment status |
| `customer list` / `purane customer` | Customer recovery list |

---

## 🎯 GOALS & TARGETS

| Command | What it does |
|---|---|
| `goal set [target]` | Set monthly goal (e.g., "goal set revenue 50000") |
| `target set [amount]` | Set revenue target |
| `progress dikhao` | Track progress vs goals |
| `daily report` | Today's progress snapshot |

---

## 🖼️ DESIGN / IMAGES

| Command | What it does |
|---|---|
| `image bana [description]` | Generate AI product/blog image |
| `blog banner bana` | Create featured image for blog |
| `ad creative bana` | Create Meta/Google ad creative |
| `carousel design` | Create carousel slides |
| `brand guidelines` | View brand colors/fonts |

---

## 💬 APPROVAL WORDS

When agency asks for your approval before making live changes:

✅ **Approve:** `yes` / `haan` / `kar do` / `theek hai` / `approve` / `go ahead`
❌ **Reject:** `no` / `nahi` / `cancel` / `ruko` / `mat karo` / `reject`

---

## 📁 FILE SHARING

**You → Agency:** Send any document/image on WhatsApp — agency will save it to `data/uploads/`
**Agency → You:** Agency sends .txt report files when big jobs are done

---

## 🔑 KEY LOCATIONS

| Item | Location |
|---|---|
| VPS | `ubuntu@140.245.246.190` |
| Agency URL | `https://falconagency.duckdns.org` |
| Website | `https://falconherbs.com` |
| Reports folder | `data/reports/` |
| Content drafts | `data/content/drafts/` |
| Product rewrites | `data/content/product_rewrites/` |
| Price data | `data/pricing/` |

---
*Falcon Agency — Your 24/7 AI Workforce 🦅*
